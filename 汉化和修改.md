# 汉化翻译内容说明

## 允许自定义数据库地址

- server/build/imp/DataBaseManagerImp.js
- 78 行 DataBaseManagerImp > class DataBaseManagerImp > async init
  ```javascript
  const host = process.env.DB_HOST;
  const port = parseInt(process.env.DB_PORT);
  const user = process.env.DB_USER;
  const password = process.env.DB_PASSWORD;
  const database = process.env.DB_NAME;
  this.pool = mariadb_1.default.createPool({
    host,
    port: port,
    user: user,
    password: password,
    connectionLimit,
    database: database,
    dateStrings: true,
  });
  ```

## 修改 Nginx 配置文件

- nginx.conf
- 注释 `resolver  XX`
- 所有 `reno_note_nodejs`，`localhost` 替换成 `127.0.0.1`
- 在`listen 80;`
  - 55行 alias 值改成 `/app/client/dist;`
  - `location /doc/client {`上面添加
    ```nginx
    location / {
      return 301 $scheme://$http_host/doc/client/;
    }

    location = /doc/client {
      return 301 $scheme://$http_host/doc/client/;
    }
    ```
- 在`listen 443 ssl;` 部分
  - 注释 `resolver 127.0.0.11 valid=30s;`
  - 注释 `location / {` 部分
  - 在上面的注释后面增加
    ```nginx
    location / {
      return 301 $scheme://$http_host/doc/client/;
    }
    ```
  - 在上面的增加后，在增加下面内容
    ```nginx
    location = /doc/client {
      return 301 $scheme://$http_host/doc/client/;
    }

    location /doc/client {
      alias /app/client/dist;
      try_files $uri $uri/ /doc/client/index.html;
      index index.html;
    }

    location /doc/server {
      proxy_pass http://127.0.0.1:8000;
    }
    ```

## 网页支持 UTF-8

- 位置 代码/client/dist/index.html
- bun ./汉化/汉化.ts 汉化/index.json 代码/client/dist/index.html

## 汉化文章编辑器

- 默认提示词
  - 查找文件
    ```bash
    grep -rl "Write some thing..." ./代码/client/dist/assets/
    ```
  - bun ./汉化/汉化.ts 汉化/文章.json 
- 格式选项
  - 位置 `代码/client/dist/quill.snow.css`
  - bun ./汉化/汉化.ts 汉化/文章-样式.json 代码/client/dist/quill.snow.css
