# 汉化翻译内容说明

## 允许自定义数据库地址

- server/build/imp/DataBaseManagerImp.js
- 78 行  DataBaseManagerImp > init() > this.pool
  ```javascript
        const host = process.env.DB_HOST;
        const port = parseInt(process.env.DB_PORT);
        const user = process.env.DB_USER;
        const password = process.env.DB_PASSWORD;
        const database = process.env.DB_NAME;
        this.pool = mariadb_1.default.createPool({
            host,
            port: port,
            user: user,
            password: password,
            connectionLimit,
            database: database,
            dateStrings: true,
        });
  ```

## 修改 Nginx 配置文件

- nginx.conf
- 41 行 注释
- 所有 `reno_note_nodejs`，`localhost` 替换成 `127.0.0.1`
- 在`listen 80;`
  - 55行 alias 值改成 `/app/client/dist;`
  - `location /doc/client {`上面添加
    ```nginx
        location / {
            return 301 $scheme://$http_host/doc/client/;
        }

        location = /doc/client {
            return 301 $scheme://$http_host/doc/client/;
        }
    ```
- 在`listen 443 ssl;`部分
  - 注释 `resolver 127.0.0.11 valid=30s;`
  - 注释 `location / {`部分
  - 在`location ~* ^/doc/websocket/port_([8][0-9][0-9][0-9])/* {`部分上面添加
    ```nginx
        location = /doc/client {
            return 301 $scheme://$http_host/doc/client/;
        }

        location /doc/client {
            alias /app/client/dist;
            try_files $uri $uri/ /doc/client/index.html;
            index index.html;
        }

        location /doc/server {
            proxy_pass http://127.0.0.1:8000;
        }
    ```
  - 在`location ~* ^/doc/websocket/port_([8][0-9][0-9][0-9])/* {`部分下面添加
    ```nginx
        location / {
            return 301 $scheme://$http_host/doc/client/;
        }
    ```

## 网页支持 UTF-8

- 代码/client/dist/index.html
  - 2 `en` => `zh-CN`
  - `head`中添加 `<meta charset="utf-8">`

## 汉化文章编辑器

- 默认提示词
  - 位置 `代码/client/dist/assets/index-DpLMh7kx.js`
  - bash ./汉化/汉化.sh 汉化/文章.json 代码/client/dist/assets/index-DpL
- 格式选项
  - 位置 `代码/client/dist/quill.snow.css`
  - bash ./汉化/汉化.sh 汉化/文章-样式.json 代码/client/dist/quill.snow
